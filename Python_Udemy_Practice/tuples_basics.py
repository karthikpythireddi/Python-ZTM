#A Tuple is like a list but we cannot modify them because they're immutable.

my_tuple = (1,2,3,4,5)

print(my_tuple[1])

print(5 in my_tuple)

#Need for Tuples:

# If we want the data to be unchanged
# They're usually faster than list

